<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">
    <t t-name="AppOne.ListView">
            <h1>Properties List</h1>
        <button class="btn btn-primary m-1 p-1" t-on-click="toggleCreateForm">
             <t t-if="state.showCreateForm">Close</t>
             <t t-else="state.showCreateForm">New</t>
        </button>
        <t t-if="state.showCreateForm">
            <FormView onRecordCreated="onRecordCreated.bind(this)" onCancel="toggleCreateForm.bind(this)"/>
        </t>
        <div class="o_list_view table-responsive">
            <table class="o_list_table table table-sm table-hover table-striped">
                <thead>
                    <tr>
                        <th class="o_list_column_header">Name</th>
                        <th class="o_list_column_header">Postcode</th>
                        <th class="o_list_column_header">Date Availability</th>
                        <th class="o_list_column_header">Actions</th>
                    </tr>
                </thead>
                <tbody>
                 <t t-foreach="state.records" t-as="record" t-key="record.id">
                    <tr>
                        <td><t t-esc="record.name"/></td>
                        <td><t t-esc="record.postcode"/></td>
                        <td><t t-esc="record.date_availability"/></td>
                        <td><button class="btn btn-danger" t-on-click="deleteRecord.bind(this,record.id)">Delete</button></td>
                    </tr>
                 </t>
                </tbody>
            </table>
        </div>
    </t>
</templates>